# Serving. `python -m venv .venv && .venv/bin/pip install -r requirements.txt`
# Interpreter: 3.9+. Pydantic models here use typing.Optional rather than `X | None`
# because pydantic resolves those annotations at runtime.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
pydantic>=2.0.0

# Training and inference
scikit-learn>=1.2.0
joblib>=1.2.0
numpy>=1.24.0
pandas>=2.0.0

# Notebooks only - not imported by backend/ or src/
matplotlib>=3.7.0
seaborn>=0.12.0
ipykernel>=6.20.0
jupyter>=1.0.0
